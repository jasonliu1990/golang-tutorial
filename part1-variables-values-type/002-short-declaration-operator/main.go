package main

import "fmt"

func main() {
	x := 42 // declare and assign variables
	fmt.Println(x)
	x = 99 // already declared, so use the equal sign to assign a new value
	fmt.Println(x)
	y := 100 + 24
	fmt.Println(y)
}
